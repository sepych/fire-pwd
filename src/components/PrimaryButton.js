import {styled} from "@material-ui/core";
import Button from "@material-ui/core/Button";

export const PrimaryButton = styled(Button)({
  background: 'linear-gradient(45deg, #FE6B8B 30%, #f73b7d 90%)',
  border: 0,
  borderRadius: 3,
  color: 'white',
  height: 38,
  padding: '0 20px',
});
