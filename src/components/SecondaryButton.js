import {styled} from "@material-ui/core";
import Button from "@material-ui/core/Button";

export const SecondaryButton = styled(Button)({
  color: 'white',
  border: 0,
  height: 38,
  padding: '0 20px',
  background: 'linear-gradient(45deg, #9d9d9d 30%, #898787 90%)',
  borderRadius: '3px',
});
